# ============================================
# XPLORIN BACKEND - ENVIRONMENT VARIABLES
# ============================================

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Untuk development lokal (XAMPP/MySQL):
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=xplorin_db
DB_PORT=3306

# Untuk production (VPS):
# DB_HOST=localhost atau IP MySQL server
# DB_USER=xplorin_user (buat user khusus, jangan root!)
# DB_PASSWORD=strong_password_here
# DB_NAME=xplorin_db
# DB_PORT=3306

# ============================================
# JWT AUTHENTICATION
# ============================================
# PENTING: Ganti dengan secret key yang kuat untuk production!
# Generate random key: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=24h

# ============================================
# SERVER CONFIGURATION
# ============================================
# Port untuk backend API
PORT=3000

# Node environment (development/production)
NODE_ENV=development

# ============================================
# CORS CONFIGURATION (Optional)
# ============================================
# Untuk production, batasi origin ke domain frontend saja
# CORS_ORIGIN=https://your-frontend-domain.com
# Untuk development, biarkan kosong atau gunakan *

# ============================================
# DEPLOYMENT NOTES:
# ============================================
# 1. WAJIB ganti JWT_SECRET dengan key yang kuat
# 2. Gunakan user database khusus (bukan root) di production
# 3. Set DB_PASSWORD yang kuat
# 4. Ganti DB_NAME sesuai nama database Anda (xplorin_db)
# 5. Untuk VPS, set NODE_ENV=production
# ============================================
